spring:
  application:
    name: product-ms

  datasource:
    url: jdbc:postgresql://localhost:5432/product_db
    username: root
    password: root
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8082
